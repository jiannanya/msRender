{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "debug",
            "type": "shell",
            "command": "cmd",
            "args": [
                "/c",
                "cd ${workspaceRoot}/nbuild && cmake .. && ninja"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}